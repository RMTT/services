apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis
  labels:
    app: {{ .Release.Name }}-redis
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: redis
  selector:
    app: {{ .Release.Name }}-redis
