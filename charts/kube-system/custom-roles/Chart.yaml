apiVersion: v2
name: prometheus-custom-roles
description: A Helm chart for creating a ServiceAccount, ClusterRole, and ClusterRoleBinding for Prometheus to access metrics in a k3s cluster.
type: application
version: 0.1.0
appVersion: "1.0.0"
