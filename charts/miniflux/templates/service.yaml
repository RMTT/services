apiVersion: v1
kind: Service
metadata:
  name: miniflux
  labels:
    app: miniflux
spec:
  type: NodePort
  selector:
    app: miniflux
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
